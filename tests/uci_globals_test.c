#include "../include/uci_globals.h"

// This translation unit intentionally left empty.
// It ensures the header can be compiled in isolation without
// duplicating the global variable definitions from src/uci_globals.c.
